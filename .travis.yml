sudo: required
services:
        - docker

before_install:
        - docker build -t vpmaddur/docker-react -f Dockerfile.dev .

script:
        - docker run -e CI=true vpmaddur/Docker-React npm run test -- --coverage
